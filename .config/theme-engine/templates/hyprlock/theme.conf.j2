# HYPRLOCK THEME CONFIGURATION
# Auto-generated by Theme Engine on {{ generated_date }}
# Source: {{ meta.name }} theme
# Do not edit manually - changes will be overwritten

# Color variables - Format: rgba(HEXCODE)
$text_color = rgba({{ app_overrides.get('text_color', '#C0CAF5') | strip_hash | add_alpha }})
$entry_background_color = rgba({{ app_overrides.get('entry_background_color', '#414868') | strip_hash | add_alpha }})
$entry_border_color = rgba({{ app_overrides.get('entry_border_color', '#9ECE6A') | strip_hash | add_alpha }})
$entry_color = rgba({{ app_overrides.get('entry_color', '#C0CAF5') | strip_hash | add_alpha }})
$check_color = rgba({{ app_overrides.get('check_color', '#BB9AF7') | strip_hash | add_alpha }})
$fail_color = rgba({{ app_overrides.get('fail_color', '#F7768E') | strip_hash | add_alpha }})
$caps_color = rgba({{ app_overrides.get('caps_color', '#E0AF68') | strip_hash | add_alpha }})
$highlight_text = rgba({{ app_overrides.get('highlight_text', '#1A1B26') | strip_hash | add_alpha }})
$background_color = rgba({{ app_overrides.get('background_color', '#1A1B26') | strip_hash | add_alpha }})
$success_color = rgba({{ app_overrides.get('success_color', '#9ECE6A') | strip_hash | add_alpha }})
$warning_color = rgba({{ app_overrides.get('warning_color', '#E0AF68') | strip_hash | add_alpha }})
$error_color = rgba({{ app_overrides.get('error_color', '#F7768E') | strip_hash | add_alpha }})

# Font family
$font_family = JetBrainsMono Nerd Font
$font_family_clock = JetBrainsMono Nerd Font Bold

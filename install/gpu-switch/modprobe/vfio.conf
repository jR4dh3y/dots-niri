# Bind the RTX GPU and its HDMI audio function to VFIO by default. The audio
# soft dependency loads vfio-pci before snd_hda_intel probes either HDA device.
options vfio-pci ids=10de:25a2,10de:2291 disable_vga=1
softdep snd_hda_intel pre: vfio-pci

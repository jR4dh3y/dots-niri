# Ly leaves the graphical seat/logind state flaky on this machine.
# When seat is not "active", WirePlumber tears down monitor.bluez and A2DP
# endpoints disappear → "Protocol not available" / "Unknown error" on headphones.
wireplumber.profiles = {
  main = {
    monitor.bluez.seat-monitoring = disabled
  }
}

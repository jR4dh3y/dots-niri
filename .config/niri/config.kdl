// Bootstrap only. The rice helper loads one of the complete profiles below:
//   config-quickshell.kdl
//   config-waybar.kdl
// Keeping this file neutral prevents either rice from briefly starting the
// other rice's bar, notification daemon, or wallpaper daemon at login.
spawn-at-startup "sh" "-c" "exec \"$HOME/.local/bin/rice\" start"
